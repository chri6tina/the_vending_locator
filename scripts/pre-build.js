// This script is intentionally empty
// We're not moving files because they need to be in the deployment
// Instead, we rely on Next.js's force-dynamic and build optimizations
console.log('Pre-build: Skipping file moves (files needed for deployment)');

